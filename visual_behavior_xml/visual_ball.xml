<root main_tree_to_execute = "MainTree" >

     <BehaviorTree ID="MainTree">
        <ReactiveSequence name="root_sequence">
            <Fallback/>
                <TryNode num_attempts="3"/>
                        <Fallback/>
                            <DetectBall turn_direction="{turn}" turn_velocity="{velocity}"/>
                            <Turn turn_direction="{turn}" turn_velocity="{velocity}"/>
                        </Fallback>
                </TryNode >
                <MakeSound/>
            </Fallback>
            <ReactiveSequence name="sub_sequence">
                <DetectBall_dist foward_direction="{foward}" foward_velocity="{velocity}"/>
                <Foward foward_direction="{foward}" foward_velocity="{velocity}"/>
            </ReactivedSequence>
        </ReactivedSequence>
     </BehaviorTree>

 </root>